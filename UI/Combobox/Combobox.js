import React, { useState } from 'react';

import classes from './Combobox.module.css';

const Combobox = (props) => {
    /**
     * Properties list
     *  - menuTemplate
     *  - itemTemplate
     *  - textElementTemplate
     *  - value
     *  - dataSource
     *      + textField
     *      + valueField
     *      + { textField: "ABC", valueField: "abc" }
     *  - placeHolder
     *  - onChange
     *  - selectionMode (No need to care yet)
     */
    return (
        <ul>
            
        </ul>
    );
}


export default Combobox;